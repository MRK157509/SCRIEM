## Known Gaps

- The repo still has some legacy/local helper code around cases that is not part of the main flow anymore.
- There is no automated end-to-end test suite yet.
- Repo hygiene still needs cleanup because the local `.venv` is noisy in git status.
